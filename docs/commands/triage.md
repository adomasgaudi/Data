<!-- Loaded when: owner types #? -->
Pre-flight triage gate: force whole-project thinking BEFORE starting, to prevent the recurring mistakes.

**`#?` <task> — the PRE-FLIGHT TRIAGE GATE. Do NOT start the task yet.** Its only job is to force the whole-project thinking the AI skips by default and so PREVENT the recurring mistakes — tunnel-vision on the one screenshot, missing the blast radius, and cramming a multi-part job into one rushed pass. Output the assessment below, then let the owner pick the path — unless step 3 lands on a genuine 1-parter, in which case say so and proceed.

1. **Zoom OUT — what does this REALLY touch?** (the AI's blind spot is the rest of the project.) List every connected piece: other tabs / views / render paths, the SSOT & data shape, locked/spectator views, `i18n.ts` (LT), version + changelog, tests, the live site / deploy, and other AIs editing right now (`#co-work`).
2. **Which "more-attention" lenses fire?** Walk each and tag APPLIES (+ one-line why) or skip: `#senior` (correctness / SSOT / invariants / irreversible change / blast radius), `#careful` (risky, many-part, or hard-to-undo), `#design` + `#ui` + `#cram` (look / placement / density / cross-section consistency), `#prune` (this is a CLASS of bug → sweep every sibling, not just the shown case), `#persistent` (a bug that has recurred), `#CEO` (a big strategic ask about the app's point/audience, not a fix), `#research` (needs best-practice grounding). Naming a lens that fires and then ignoring it is exactly the mistake this gate exists to catch.
3. **How many PARTS?** Recommend the split + the count: **1** = trivial, one clean pass (say so, proceed) · **2–5** = list each part as its own step (usually → run it as `#careful`) · **~100 / strategic** = it's a `#CEO`, write the plan MD first. NEVER cram a multi-part job into one pass — that is what drops parts.
4. **VERDICT (one line):** proceed now, or escalate to [the lenses/commands that fired] first — then let the owner choose. The goal is not speed; it's not making the avoidable mistake.
