<!-- Loaded when: owner types #CEO -->
Purpose: ZOOM OUT — don't fix the bug; judge the request against the POINT of the app, its hook, and its audience; write a ~100-prompt plan MD, then consult the owner before executing.

# #CEO <problem>

ZOOM OUT, don't fix the bug. Judge the request against the POINT of the app (Colosseum), its marketing/curiosity hook, and the target audience across 4 tiers:
- ① **Curious-but-uninterested** — need a SHOCKING/surprising stat to make them click.
- ② **Interested** — make controls EASY.
- ③ **Nerds** — deep tinkering.
- ④ **Actionable** — turns into a next action.

This is a ~100-prompt question — too big for one reply, so DON'T try. Instead:
1. Write the CEO question + a numbered ~100-prompt plan into its own `docs/ceo/<slug>.md` (survives AI discontinuity — any later AI picks up tasks straight from the MD).
2. CONSULT the owner to approve/adjust the plan BEFORE executing.

Spec + template: `docs/ceo/README.md`.
